
import Handlers from './handlers/index.js';

const ROUTE_ACCEPT_RULES = '/accept-rules';
const ROUTE_PROMOTE_GUEST = '/promote-guest';

export default {
    Handlers,
    routes: {
        ROUTE_ACCEPT_RULES: ROUTE_ACCEPT_RULES,
        ROUTE_PROMOTE_GUEST: ROUTE_PROMOTE_GUEST,
    }
};